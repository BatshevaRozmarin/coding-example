on:
  push:
    branches:
     - calculator
jobs:
  test-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
              npm ci
              npm run build
              npm run test:unit
              npm run lint
defaults:
  run:
    working-directory: calculator-shevi/tests